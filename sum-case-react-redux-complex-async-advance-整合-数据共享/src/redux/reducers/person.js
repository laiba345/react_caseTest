import { ADD_PERSON } from '../'
const initState = [{ id: '001', name: 'tom', age: 19 }]
export default function personReducer(preState, action) {
    const { type, data } = action
    switch(type) {
        case
    }

}